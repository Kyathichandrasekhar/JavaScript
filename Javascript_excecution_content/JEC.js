//java excecution content

//the execution content is a script that will be executed in the browser context, it can be used to manipulate the DOM, interact with the page, or perform any other actions that are possible in a browser environment.

//How the excecution content works is that it is injected into the page and executed as if it were part of the page's own JavaScript code.
//  This means that it has access to the same variables, functions, and objects that are available in the page's context.

//Excecution phase : 
// 1. The browser loads the HTML document and parses it into a DOM (Document Object Model) tree.
// 2. The browser then loads and executes any JavaScript code that is included in the HTML document, either inline or in external files.
// 3. The JavaScript code can manipulate the DOM, interact with the page, and perform any other actions that are possible in a browser environment.
// 4. Once the JavaScript code has finished executing, the browser continues to render the page and respond to user interactions.
